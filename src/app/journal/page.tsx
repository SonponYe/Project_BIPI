// Private reflection entries. Never aggregated into BIPI Pulse (pitch
// Section 4). This is a placeholder listing view — entries are written from
// within individual reflection-journal modules.
export default function JournalPage() {
  return (
    <main className="mx-auto max-w-xl p-6">
      <h1 className="text-xl font-bold text-pulse-800">Your journal</h1>
      <p className="mt-2 text-gray-500">
        Entries you save while reflecting will appear here, visible only to you.
      </p>
    </main>
  );
}
